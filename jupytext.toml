# Pair every notebook with a percent-format script, per AGENTS.md section 6.
# Author in the .py; run `jupytext --sync notebooks/<name>.ipynb` afterwards.
default_jupytext_formats = "ipynb,py:percent"
